char array to String
	String str = String.valueOf(char[] charArr);

void Arrays.sort(<array>)

ArrayList<T> al = new ArrayList<T>(<Integer>, cannot put others here);
		THIS IS WRONG if ....new ArrayList<T>("Hello");

iterate hashmap values
	Collection<V> values();
	hm.values();
